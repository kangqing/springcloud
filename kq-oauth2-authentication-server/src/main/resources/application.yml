server:
  port: 7027
spring:
  profiles:
    active: dev
  application:
    name: kq-oauth2-authentication-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
management:
  endpoints:
    web:
      exposure:
        include: "*"
